import { OverviewLengthDirective } from '../directives/overview-length.directive';

describe('OverviewLengthDirective', () => {
  it('should create an instance', () => {
    const directive = new OverviewLengthDirective();
    expect(directive).toBeTruthy();
  });
});
